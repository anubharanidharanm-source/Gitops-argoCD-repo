apiVersion: v1
kind: Service
metadata:
  name: portfolio-web-lb
  labels:
    app: portfolio-web
spec:
  type: LoadBalancer
  selector:
    app: portfolio-web
  ports:
    - protocol: TCP
      port: 80      
      targetPort: 80 