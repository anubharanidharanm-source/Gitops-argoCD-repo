apiVersion: v1
kind: Service
metadata:
  name: portfolio-web-svc
  labels:
    app: portfolio-web
spec:
  type: ClusterIP
  selector:
    app: portfolio-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80